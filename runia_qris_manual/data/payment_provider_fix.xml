<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="payment_provider_qris_manual" model="payment.provider">
            <field name="is_published">True</field>
            <field name="state">test</field>
            <!-- Unrestrict Everything -->
            <!-- company_id MUST NOT be False if required by DB schema -->
            <!-- <field name="company_id" eval="False"/> -->
            <field name="website_id" eval="False"/>
            <field name="available_country_ids" eval="[(5, 0, 0)]"/>
            <field name="available_currency_ids" eval="[(5, 0, 0)]"/>
            <field name="maximum_amount">0.0</field>
            <!-- Ensure method is linked -->
            <field name="payment_method_ids" eval="[(6, 0, [ref('payment_method_qris_manual')])]"/>
        </record>

    </data>
</odoo>
